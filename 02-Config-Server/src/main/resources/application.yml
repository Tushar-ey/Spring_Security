server:
  port: 1111

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Tushar-ey/configuration_properties.git
          clone-on-start: true
          
management:
  endpoints:
    web:
      exposure:
        include: '*'